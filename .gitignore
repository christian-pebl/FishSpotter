# dependencies
node_modules
.pnp
.pnp.js

# build
.next
out
build
dist

# env
.env
.env.local
.env.*.local

# bootstrap kit (operator-only secrets file)
scripts/bootstrap/tokens.json

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# db
prisma/*.db
prisma/*.db-journal

# IDE
.idea
.vscode
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local DB backups (raw row dumps; never commit)
backups/

# playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# lighthouse-ci
.lighthouseci/

# storybook
storybook-static/

# Claude Code worktrees + session files
.claude/

# Supabase CLI temp files
supabase/.temp/

# Windows null device artifact
nul

# Local DB backups (large binary dumps)
backup*/
*.backup
*.backup.gz

# TypeScript incremental build info
tsconfig.tsbuildinfo

# Spot It reference guide PDFs (48MB of free UK marine ID guides; source
# material for catalogue authoring, re-downloadable, not versioned)
decision-tree/id-guides/

# Gemini image-quality sweep outputs (local only)
assess-*.json
assess-*.log

# Local scratch (preview tmp, PR-body drafts, ad-hoc render output)
.pc-tmp/
.pc-*.txt
audit-species-images.json
implementation/2026-06-04/orientation-audit.json
